/**
 * Placeholder for generated Supabase types.
 *
 * Once migrations in supabase/migrations are applied to the linked project,
 * generate real types and replace this file with:
 *
 *   npx supabase gen types typescript --project-id ubvzdglrwkkuaigmkjap > src/lib/supabase/types.ts
 *
 * Until then the client is typed loosely (not narrowed per-table) so
 * `.from("table_name")` calls don't hard-fail the build. This is
 * intentionally temporary — do not hand-write table types here.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
